# 歌词功能 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为音乐播放器添加 LRC 歌词功能 — 独立弹出窗口 + 时间轴同步高亮 + 两种显示模式 + 数据库存储。

**Architecture:** songs 表新增 `lrc_text` 列存储 LRC 字符串。新增 `GET /api/lyrics/:songId` 端点。主窗口 player.js 通过 BroadcastChannel 向独立歌词窗口推送播放时间。歌词窗口 (`lyrics.html` + `js/lyrics.js`) 解析 LRC、渲染高亮、支持竖/横两种模式。

**Tech Stack:** Node.js + Express + Supabase PostgreSQL + 原生 JS (IIFE) + BroadcastChannel + CSS 变量

## Global Constraints

- 项目无测试框架，验证方式为启动 server + 浏览器手动验证
- 前端使用原生 JS IIFE 模块模式，与现有 `Auth`/`PlaylistStore`/`Player`/`UI` 保持一致
- 歌词窗口使用 `window.open()` 独立弹出，不依赖任何第三方库
- LRC 格式：`[mm:ss.xx]歌词文本`
- 设计系统保持现有 CSS 变量（`--accent`, `--bg-card` 等暖色系）

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `sql/alter_lyrics.sql` | **新增** | 已有数据库的 ALTER TABLE（在 Supabase SQL Editor 中执行） |
| `sql/schema.sql` | **修改** | 在建表语句中追加 `lrc_text` 列（供新建库使用） |
| `server.js` | **修改** | 新增 `GET /api/lyrics/:songId` 端点 |
| `js/player.js` | **修改** | 新增 BroadcastChannel 推送（timeupdate + 歌曲切换） |
| `js/ui.js` | **修改** | 新增歌词按钮、BroadcastChannel 监听、歌词获取逻辑 |
| `lyrics.html` | **新增** | 独立歌词弹出窗口页面 |
| `js/lyrics.js` | **新增** | 歌词窗口逻辑：LRC 解析、渲染、同步、模式切换 |
| `css/lyrics.css` | **新增** | 歌词窗口样式（独立文件，lyrics.html 内联也可） |
| `index.html` | **修改** | 底部播放栏追加歌词按钮 `🎤` |
| `scripts/fetch_lyrics.js` | **新增** | 歌词自动匹配脚本 |

**边界与接口：**

- `player.js` 不依赖 `ui.js` 或 `lyrics.js` — 它只向 BroadcastChannel 写入
- `ui.js` 通过 BroadcastChannel 感知歌词窗口开关状态
- `lyrics.js` 完全自包含 — 它从 BroadcastChannel 接收数据，自行 fetch `/api/lyrics/:id`
- 不出现在 HTML 中的 `<script>` 加载顺序问题：`lyrics.js` 是独立窗口，不通过 `index.html` 加载

---

### Task 1: 数据库 — 新增 lrc_text 列

**Files:**
- Create: `sql/alter_lyrics.sql`
- Modify: `sql/schema.sql` (在 songs 表 DDL 中追加列)

**Interfaces:**
- Produces: `songs.lrc_text` 列 (TEXT DEFAULT NULL)，供 Task 2 的 `/api/lyrics/:songId` 查询

- [ ] **Step 1: 创建 alter_lyrics.sql（已有数据库执行用）**

```sql
-- 为已有数据库添加歌词列
-- 在 Supabase SQL Editor 中执行此文件
ALTER TABLE songs ADD COLUMN IF NOT EXISTS lrc_text TEXT DEFAULT NULL;
COMMENT ON COLUMN songs.lrc_text IS 'LRC 格式歌词，每行 [mm:ss.xx]lyric，NULL 表示暂无歌词';
```

- [ ] **Step 2: 修改 schema.sql，在 songs 表 DDL 中追加列**

在 `sql/schema.sql` 的 `songs` 表 `CREATE TABLE` 语句中，`created_at` 列之后追加：

```sql
    lrc_text        TEXT         DEFAULT NULL,           -- LRC 格式歌词
```

同时在 COMMENT 区域追加：

```sql
COMMENT ON COLUMN songs.lrc_text IS 'LRC 格式歌词，每行 [mm:ss.xx]lyric，NULL 表示暂无歌词';
```

修改位置：
- `sql/schema.sql:55`（在 `created_at TIMESTAMPTZ ...` 行之后插入 `lrc_text` 列）
- `sql/schema.sql`（在 `COMMENT ON COLUMN songs.duration_seconds` 之后追加 comment）

- [ ] **Step 3: 在 Supabase SQL Editor 执行 alter_lyrics.sql**

打开 Supabase Dashboard → SQL Editor → 粘贴 `sql/alter_lyrics.sql` 内容 → Run。

验证：
```bash
# 确认列已添加
SERVICE_KEY="<SUPABASE_SERVICE_ROLE_KEY>"
curl -s "https://orphftlwdwuvoscizndx.supabase.co/rest/v1/songs?select=id,title,lrc_text&limit=3" \
  -H "apikey: $SERVICE_KEY" -H "Authorization: Bearer $SERVICE_KEY"
```
预期：返回结果中包含 `lrc_text` 字段（值为 null）。

- [ ] **Step 4: Commit**

```bash
git add sql/alter_lyrics.sql sql/schema.sql
git commit -m "feat: add lrc_text column to songs table"
```

---

### Task 2: 后端 — GET /api/lyrics/:songId 端点

**Files:**
- Modify: `server.js`（新增端点，放在 `/api/stream/:songId` 之后、Auth 端点之前）

**Interfaces:**
- Consumes: `songs.lrc_text` 列（Task 1）
- Produces: `GET /api/lyrics/:songId` → `200 { songId, title, singer, lrc_text }` 或 `404 { error }`

- [ ] **Step 1: 在 server.js 中添加歌词端点**

在 `server.js` 的 `// ========== Auth 端点 ==========` 行之前插入以下代码：

```javascript
/** GET /api/lyrics/:songId — 获取歌曲 LRC 歌词 */
app.get('/api/lyrics/:songId', async (req, res) => {
    const songId = parseInt(req.params.songId);
    if (!songId || songId < 1) {
        return res.status(400).json({ error: '无效的歌曲 ID' });
    }

    try {
        const { data, error } = await supabase
            .from('songs')
            .select('id, title, singer, lrc_text')
            .eq('id', songId)
            .single();

        if (error || !data) {
            return res.status(404).json({ error: '歌曲不存在' });
        }

        res.json({
            songId: data.id,
            title: data.title,
            singer: data.singer || '',
            lrc_text: data.lrc_text || null,
        });
    } catch (err) {
        console.error('[lyrics]', err.message);
        res.status(500).json({ error: '获取歌词失败' });
    }
});
```

- [ ] **Step 2: 启动服务器并验证端点**

```bash
# 启动服务器
/d/softwa/nodejs/node server.js &

# 验证正常歌曲（有歌词或无歌词）
curl -s "http://localhost:8765/api/lyrics/1" | head -c 200

# 验证不存在的歌曲
curl -s "http://localhost:8765/api/lyrics/99999"
```

预期：
- 歌曲1 返回 `{"songId":1,"title":"...","singer":"...","lrc_text":null}`（初始无歌词）
- 歌曲99999 返回 `{"error":"歌曲不存在"}` + 状态码 404

- [ ] **Step 3: Commit**

```bash
git add server.js
git commit -m "feat: add GET /api/lyrics/:songId endpoint"
```

---

### Task 3: 前端 — 歌词弹出窗口 (lyrics.html + js/lyrics.js + css/lyrics.css)

**Files:**
- Create: `lyrics.html`
- Create: `js/lyrics.js`
- Create: `css/lyrics.css`

**Interfaces:**
- Consumes: BroadcastChannel `music_player_lyrics`（接收 `time-update`、`song-change`、`lyrics-open` 消息）
- Consumes: `GET /api/lyrics/:songId`（Task 2）
- Produces: BroadcastChannel 发送 `lyrics-closed`、`mode-change`
- Produces: 自包含页面，URL 参数 `?songId=N` 可选

- [ ] **Step 1: 创建 css/lyrics.css**

```css
/* ============================================
   歌词弹出窗口样式
   ============================================ */

:root {
    --bg-primary: #F5E6D3;
    --bg-card: #FFFDF9;
    --accent: #E8917B;
    --accent-light: #fce4de;
    --text-primary: #4A3F3B;
    --text-secondary: #8B7D78;
    --text-muted: #b0a59f;
    --border: #ede4da;
    --radius-sm: 10px;
    --radius-md: 16px;
    --transition: 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    --font-stack: -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

html, body {
    height: 100%;
    overflow: hidden;
    background: transparent;
    font-family: var(--font-stack);
    user-select: none;
    -webkit-user-select: none;
}

/* 主容器 — 毛玻璃效果 */
.lyrics-container {
    height: 100%;
    display: flex;
    flex-direction: column;
    background: rgba(255, 253, 249, 0.92);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-radius: 12px;
    overflow: hidden;
}

/* 标题栏 — 可拖拽区域 */
.lyrics-header {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 10px 14px;
    cursor: move;
    flex-shrink: 0;
    border-bottom: 1px solid var(--border);
}

.lyrics-header .song-info {
    flex: 1;
    min-width: 0;
}

.lyrics-header .song-title {
    font-size: 14px;
    font-weight: 600;
    color: var(--text-primary);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.lyrics-header .song-singer {
    font-size: 11px;
    color: var(--text-muted);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.lyrics-header button {
    width: 28px;
    height: 28px;
    border: none;
    background: transparent;
    cursor: pointer;
    border-radius: 50%;
    font-size: 13px;
    color: var(--text-muted);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: color var(--transition), background var(--transition);
    flex-shrink: 0;
}

.lyrics-header button:hover {
    color: var(--accent);
    background: var(--accent-light);
}

/* 歌词区域 */
.lyrics-body {
    flex: 1;
    overflow: hidden;
    position: relative;
    padding: 20px 16px;
}

/* ====== 竖条形模式 (vertical — 10行) ====== */
.lyrics-vertical {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 14px;
    transition: transform 0.4s ease;
}

.lyrics-vertical .lyric-line {
    font-size: 15px;
    color: var(--text-muted);
    text-align: center;
    transition: all 0.35s ease;
    padding: 4px 8px;
    border-radius: 8px;
    line-height: 1.6;
}

.lyrics-vertical .lyric-line.active {
    font-size: 20px;
    font-weight: 700;
    color: var(--accent);
    background: var(--accent-light);
    transform: scale(1.02);
}

/* ====== 长条形模式 (horizontal — 2行) ====== */
.lyrics-horizontal {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 20px;
}

.lyrics-horizontal .lyric-line {
    font-size: 16px;
    color: var(--text-muted);
    text-align: center;
    transition: all 0.4s ease;
    padding: 6px 12px;
    border-radius: 8px;
    line-height: 1.6;
    max-width: 100%;
}

.lyrics-horizontal .lyric-line.active {
    font-size: 32px;
    font-weight: 700;
    color: var(--accent);
}

.lyrics-horizontal .lyric-line.next-line {
    font-size: 18px;
    color: var(--text-secondary);
    opacity: 0.7;
}

/* 无歌词状态 */
.lyrics-empty {
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: var(--text-muted);
    font-size: 14px;
    gap: 8px;
}

.lyrics-empty .empty-icon {
    font-size: 40px;
    opacity: 0.5;
}
```

- [ ] **Step 2: 创建 js/lyrics.js**

```javascript
/**
 * lyrics.js — 歌词弹出窗口逻辑
 * =================================
 * 在 lyrics.html 独立窗口中运行。
 * 通过 BroadcastChannel 与主窗口通信。
 */

const Lyrics = (() => {
    // ========== 状态 ==========
    let mode = 'vertical';       // 'vertical' | 'horizontal'
    let lines = [];              // [{ time: number, text: string }, ...]
    let currentLineIdx = -1;
    let songTitle = '';
    let songSinger = '';

    // ========== DOM 引用 ==========
    let elHeader, elTitle, elSinger, elBody, elBtnMode, elBtnClose;

    function cacheDom() {
        elHeader  = document.getElementById('lyricsHeader');
        elTitle   = document.getElementById('lyricsTitle');
        elSinger  = document.getElementById('lyricsSinger');
        elBody    = document.getElementById('lyricsBody');
        elBtnMode = document.getElementById('btnMode');
        elBtnClose= document.getElementById('btnClose');
    }

    // ========== LRC 解析 ==========

    function parseLRC(lrcText) {
        if (!lrcText) return [];
        const result = [];
        const lines = lrcText.split('\n');
        const timeRe = /\[(\d{2}):(\d{2})\.(\d{2,3})\](.*)/;

        for (const line of lines) {
            const m = line.match(timeRe);
            if (!m) continue;
            const minutes = parseInt(m[1], 10);
            const seconds = parseInt(m[2], 10);
            const ms = parseInt(m[3].padEnd(3, '0'), 10);
            const time = minutes * 60 + seconds + ms / 1000;
            const text = m[4].trim();
            if (text) {
                result.push({ time, text });
            }
        }

        result.sort((a, b) => a.time - b.time);
        return result;
    }

    // ========== 渲染 ==========

    function render() {
        if (!elBody) return;

        if (lines.length === 0) {
            elBody.innerHTML = `
                <div class="lyrics-empty">
                    <div class="empty-icon">🎵</div>
                    <div>暂无歌词</div>
                </div>`;
            return;
        }

        if (mode === 'vertical') {
            renderVertical();
        } else {
            renderHorizontal();
        }
    }

    function renderVertical() {
        // 显示当前行前后各 4~5 行，共约 10 行
        const visibleCount = 10;
        const halfCount = Math.floor(visibleCount / 2);
        let startIdx = Math.max(0, currentLineIdx - halfCount);
        const endIdx = Math.min(lines.length, startIdx + visibleCount);

        // 尽量填满 10 行
        if (endIdx - startIdx < visibleCount) {
            startIdx = Math.max(0, endIdx - visibleCount);
        }

        const visibleLines = lines.slice(startIdx, endIdx);

        elBody.innerHTML = `
            <div class="lyrics-vertical" style="transform: translateY(${-Math.max(0, currentLineIdx - startIdx - halfCount) * 0}px)">
                ${visibleLines.map((l, i) => {
                    const globalIdx = startIdx + i;
                    const cls = globalIdx === currentLineIdx ? 'lyric-line active' : 'lyric-line';
                    return `<div class="${cls}" data-idx="${globalIdx}">${escapeHtml(l.text)}</div>`;
                }).join('')}
            </div>`;
    }

    function renderHorizontal() {
        const currentLine = lines[currentLineIdx] || null;
        const nextLine = currentLineIdx >= 0 && currentLineIdx < lines.length - 1
            ? lines[currentLineIdx + 1] : null;

        elBody.innerHTML = `
            <div class="lyrics-horizontal">
                <div class="lyric-line active">${currentLine ? escapeHtml(currentLine.text) : '♪'}</div>
                <div class="lyric-line next-line">${nextLine ? escapeHtml(nextLine.text) : ''}</div>
            </div>`;
    }

    function escapeHtml(str) {
        const d = document.createElement('div');
        d.textContent = str;
        return d.innerHTML;
    }

    // ========== 时间同步 ==========

    function syncTime(currentSec) {
        if (lines.length === 0) return;

        // 二分查找当前行：最后一个 time <= currentSec 的行
        let lo = 0, hi = lines.length - 1;
        let found = -1;
        while (lo <= hi) {
            const mid = (lo + hi) >> 1;
            if (lines[mid].time <= currentSec) {
                found = mid;
                lo = mid + 1;
            } else {
                hi = mid - 1;
            }
        }

        if (found !== currentLineIdx) {
            currentLineIdx = found;
            render();
        }
    }

    // ========== 歌曲切换 ==========

    async function loadSong(songId) {
        try {
            const resp = await fetch(`/api/lyrics/${songId}`);
            if (!resp.ok) {
                lines = [];
                currentLineIdx = -1;
                render();
                return;
            }
            const data = await resp.json();
            songTitle = data.title || '';
            songSinger = data.singer || '';

            if (elTitle) elTitle.textContent = songTitle;
            if (elSinger) elSinger.textContent = songSinger;

            lines = parseLRC(data.lrc_text);
            currentLineIdx = -1;
            render();
        } catch (err) {
            console.error('[lyrics] 加载歌词失败:', err);
            lines = [];
            currentLineIdx = -1;
            render();
        }
    }

    // ========== 模式切换 ==========

    function toggleMode() {
        mode = mode === 'vertical' ? 'horizontal' : 'vertical';
        if (elBtnMode) {
            elBtnMode.textContent = mode === 'vertical' ? '≡' : '—';
            elBtnMode.title = mode === 'vertical' ? '竖条形模式' : '长条形模式';
        }
        render();

        // 通知主窗口
        try {
            const bc = new BroadcastChannel('music_player_lyrics');
            bc.postMessage({ type: 'mode-change', mode });
            bc.close();
        } catch {}
    }

    // ========== 拖拽 ==========

    function setupDrag() {
        if (!elHeader) return;
        let dragging = false;
        let startX, startY, winX, winY;

        elHeader.addEventListener('mousedown', (e) => {
            if (e.target.closest('button')) return;
            dragging = true;
            startX = e.screenX;
            startY = e.screenY;
            winX = window.screenX;
            winY = window.screenY;
            document.body.style.cursor = 'move';
        });

        document.addEventListener('mousemove', (e) => {
            if (!dragging) return;
            const dx = e.screenX - startX;
            const dy = e.screenY - startY;
            window.moveTo(winX + dx, winY + dy);
        });

        document.addEventListener('mouseup', () => {
            if (dragging) {
                dragging = false;
                document.body.style.cursor = '';
            }
        });
    }

    // ========== BroadcastChannel 监听 ==========

    function setupChannel() {
        const bc = new BroadcastChannel('music_player_lyrics');

        bc.onmessage = (e) => {
            const msg = e.data;
            if (!msg || !msg.type) return;

            switch (msg.type) {
                case 'time-update':
                    syncTime(msg.currentTime || 0);
                    break;

                case 'lyrics-open':
                case 'song-change':
                    if (msg.id) {
                        loadSong(msg.id);
                    }
                    break;
            }
        };
    }

    // ========== 事件绑定 ==========

    function setupEvents() {
        if (elBtnClose) {
            elBtnClose.addEventListener('click', () => {
                try {
                    const bc = new BroadcastChannel('music_player_lyrics');
                    bc.postMessage({ type: 'lyrics-closed' });
                    bc.close();
                } catch {}
                window.close();
            });
        }

        if (elBtnMode) {
            elBtnMode.addEventListener('click', toggleMode);
        }
    }

    // ========== 初始化 ==========

    function init() {
        cacheDom();
        setupDrag();
        setupChannel();
        setupEvents();

        // 检查 URL 参数中是否有 songId，有则自动加载
        const urlParams = new URLSearchParams(window.location.search);
        const songId = urlParams.get('songId');
        if (songId) {
            loadSong(parseInt(songId, 10));
        } else {
            render();
        }

        // 设置初始按钮文字
        if (elBtnMode) {
            elBtnMode.textContent = mode === 'vertical' ? '≡' : '—';
        }
    }

    // DOM ready 后初始化
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }

    return { parseLRC, syncTime, toggleMode, loadSong };
})();
```

- [ ] **Step 3: 创建 lyrics.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🎵 歌词</title>
    <link rel="stylesheet" href="css/lyrics.css">
</head>
<body>
    <div class="lyrics-container">
        <!-- 标题栏（可拖拽） -->
        <div class="lyrics-header" id="lyricsHeader">
            <div class="song-info">
                <div class="song-title" id="lyricsTitle">未在播放</div>
                <div class="song-singer" id="lyricsSinger"></div>
            </div>
            <button id="btnMode" title="切换模式">≡</button>
            <button id="btnClose" title="关闭">✕</button>
        </div>

        <!-- 歌词区域 -->
        <div class="lyrics-body" id="lyricsBody">
            <div class="lyrics-empty">
                <div class="empty-icon">🎵</div>
                <div>等待播放...</div>
            </div>
        </div>
    </div>

    <script src="js/lyrics.js"></script>
</body>
</html>
```

- [ ] **Step 4: 验证歌词窗口**

手动验证步骤：
1. 启动服务器：`/d/softwa/nodejs/node server.js`
2. 浏览器打开 `http://localhost:8765`
3. 在浏览器控制台执行以下代码模拟打开歌词窗口：

```javascript
// 打开歌词窗口
const lw = window.open('http://localhost:8765/lyrics.html?songId=1', 'lyricsWin', 'width=360,height=520');
```

4. 验证窗口打开、标题栏可拖拽、无歌词时显示"暂无歌词"
5. 在控制台模拟发送时间同步：

```javascript
// 在歌词窗口仍在打开时，从主窗口广播
const bc = new BroadcastChannel('music_player_lyrics');
bc.postMessage({ type: 'lyrics-open', id: 1 });
bc.postMessage({ type: 'time-update', currentTime: 30.5 });
```

- [ ] **Step 5: Commit**

```bash
git add lyrics.html js/lyrics.js css/lyrics.css
git commit -m "feat: add lyrics popup window with vertical/horizontal modes"
```

---

### Task 4: Player 集成 — BroadcastChannel 推送

**Files:**
- Modify: `js/player.js`（在 `init()` 和 `load()` 中添加 BroadcastChannel 推送）

**Interfaces:**
- Consumes: 现有 `emit()` 事件系统
- Produces: BroadcastChannel `music_player_lyrics` 发送 `time-update` 和 `song-change` 消息

- [ ] **Step 1: 修改 player.js — 在初始化时创建 BroadcastChannel**

在 `player.js` 顶部（`const Player = (() => {` 之后）添加 BroadcastChannel 初始化：

```javascript
// ========== 歌词同步通道 ==========
let lyricsChannel = null;
try {
    lyricsChannel = new BroadcastChannel('music_player_lyrics');
} catch (e) {
    // BroadcastChannel 不可用（旧浏览器），静默降级
}
```

- [ ] **Step 2: 修改 player.js — 在 timeupdate 监听器中推送时间**

找到 `player.js` 中 `audio.addEventListener('timeupdate', ...)` 内的 `emit('timeupdate', ...)` 调用之后，追加歌词同步逻辑。

具体位置：`player.js` 约第 92 行，`emit('timeupdate', { ... })` 的闭括号 `}` 之后。

```javascript
            // 歌词同步：推送当前播放时间
            if (lyricsChannel) {
                try {
                    lyricsChannel.postMessage({
                        type: 'time-update',
                        currentTime: displayTime,  // 已扣除 startTime 偏移
                    });
                } catch {}
            }
```

注意：使用 `displayTime`（已扣除 `startTime` 偏移的显示时间），这样歌词同步的是歌曲本身的进度，不包含片头偏移。

- [ ] **Step 3: 修改 player.js — 在 load() 中推送歌曲切换**

找到 `player.js` 中 `load()` 函数，在 `currentSong = song;` 之后、`emit('loading', song);` 之前追加歌曲切换推送：

```javascript
        // 歌词同步：推送歌曲切换
        if (lyricsChannel) {
            try {
                lyricsChannel.postMessage({
                    type: 'song-change',
                    id: song.id,
                });
            } catch {}
        }
```

- [ ] **Step 4: 验证**

手动验证：
1. 启动服务器，打开 `http://localhost:8765`
2. 播放一首歌（点击歌曲卡片）
3. 打开歌词窗口：`window.open('http://localhost:8765/lyrics.html', 'lyricsWin', 'width=360,height=520')`
4. 验证歌词窗口收到 `song-change` 消息后自动请求 `/api/lyrics/:id`
5. 验证播放时歌词窗口收到 `time-update`（打开歌词窗口控制台查看消息）

- [ ] **Step 5: Commit**

```bash
git add js/player.js
git commit -m "feat: add BroadcastChannel push from player to lyrics window"
```

---

### Task 5: UI 集成 — 歌词按钮 + 打开/关闭逻辑

**Files:**
- Modify: `index.html`（底部播放栏新增歌词按钮）
- Modify: `js/ui.js`（歌词按钮事件、BroadcastChannel 监听）

**Interfaces:**
- Consumes: BroadcastChannel 接收 `lyrics-closed`、`mode-change`
- Produces: `window.open()` 打开 `lyrics.html`，BroadcastChannel 发送 `lyrics-open`

- [ ] **Step 1: 修改 index.html — 添加歌词按钮**

在 `index.html` 底部播放栏的 `controls-row` 中，在 `btnMode` 之前添加歌词按钮：

```html
            <button class="btn-ctrl" id="btnLyrics" title="歌词">🎤</button>
```

位置：`index.html` 约第 70 行，`<button class="btn-mode ..."` 之前。

- [ ] **Step 2: 修改 ui.js — 在 cacheDom() 中添加歌词按钮引用**

在 `ui.js` 的 `cacheDom()` 函数中，`els = {` 对象内追加：

```javascript
                btnLyrics: document.getElementById('btnLyrics'),
```

- [ ] **Step 3: 修改 ui.js — 添加歌词窗口管理逻辑**

在 `ui.js` 顶部（`const UI = (() => {` 之后，`let els = {};` 之前），添加歌词窗口状态：

```javascript
    let lyricsWindow = null;  // 歌词弹出窗口引用
```

在 `ui.js` 底部（`setupGlobalListeners` 函数之前），添加歌词窗口管理函数：

```javascript
    // ========== 歌词窗口管理 ==========

    function openLyricsWindow() {
        // 如果窗口已存在且未关闭，聚焦
        if (lyricsWindow && !lyricsWindow.closed) {
            lyricsWindow.focus();
            return;
        }

        const song = Player.getCurrentSong();
        const songId = song ? song.id : '';

        lyricsWindow = window.open(
            `/lyrics.html?songId=${songId}`,
            'lyricsWin',
            'width=360,height=520,menubar=no,toolbar=no,location=no,status=no'
        );

        if (!lyricsWindow) {
            alert('歌词窗口被浏览器拦截，请允许弹窗后重试');
            return;
        }

        // 等窗口加载后发送初始歌曲信息
        lyricsWindow.addEventListener('load', () => {
            if (song) {
                try {
                    const bc = new BroadcastChannel('music_player_lyrics');
                    bc.postMessage({
                        type: 'lyrics-open',
                        id: song.id,
                    });
                    bc.close();
                } catch {}
            }
        });
    }

    function closeLyricsWindow() {
        if (lyricsWindow && !lyricsWindow.closed) {
            lyricsWindow.close();
        }
        lyricsWindow = null;
    }

    function setupLyricsChannel() {
        try {
            const bc = new BroadcastChannel('music_player_lyrics');
            bc.onmessage = (e) => {
                const msg = e.data;
                if (!msg || !msg.type) return;

                switch (msg.type) {
                    case 'lyrics-closed':
                        lyricsWindow = null;
                        break;
                    case 'mode-change':
                        // 可存储用户偏好，当前不需要
                        break;
                }
            };
        } catch {}
    }
```

- [ ] **Step 4: 修改 ui.js — 在 init() 中调用 setupLyricsChannel()**

在 `ui.js` 的 `init()` 函数中，`setupGlobalListeners();` 之前添加：

```javascript
        setupLyricsChannel();
```

- [ ] **Step 5: 修改 ui.js — 在 setupGlobalListeners() 中绑定歌词按钮**

在 `setupGlobalListeners()` 函数中，播放器控制按钮部分（`if (els.btnPlay)` 等附近），添加：

```javascript
        if (els.btnLyrics) els.btnLyrics.addEventListener('click', () => openLyricsWindow());
```

- [ ] **Step 6: 添加 window unload 时关闭歌词窗口**

在 `ui.js` 的 `init()` 函数末尾添加：

```javascript
        // 主窗口关闭时自动关闭歌词窗口
        window.addEventListener('beforeunload', () => {
            closeLyricsWindow();
        });
```

- [ ] **Step 7: 验证**

手动验证：
1. 启动服务器，打开 `http://localhost:8765`
2. 点击底部播放栏的 `🎤` 歌词按钮
3. 应弹出歌词窗口 (360×520)，关闭歌词窗口
4. 先播放一首歌，再打开歌词窗口 — 验证窗口自动加载当前歌曲歌词
5. 关闭主窗口 — 验证歌词窗口也关闭

- [ ] **Step 8: Commit**

```bash
git add index.html js/ui.js
git commit -m "feat: add lyrics button and window open/close logic"
```

---

### Task 6: 歌词自动匹配脚本

**Files:**
- Create: `scripts/fetch_lyrics.js`

**Interfaces:**
- Consumes: Supabase REST API (service_role key) 查询无歌词歌曲 + 更新 lrc_text
- Produces: 控制台日志输出匹配结果

- [ ] **Step 1: 创建 scripts/fetch_lyrics.js**

```javascript
/**
 * fetch_lyrics.js — 为数据库中的歌曲自动匹配 LRC 歌词
 * =====================================================
 * 用法: /d/softwa/nodejs/node scripts/fetch_lyrics.js
 *
 * 流程:
 *   1. 查询所有 lrc_text IS NULL 的歌曲
 *   2. 对每首歌，调用公开 LRC API 搜索
 *   3. 校验歌词（至少 5 行时间戳行）
 *   4. 写入数据库
 */

const path = require('path');
const fs = require('fs');

// ========== 加载 .env ==========
(function loadEnv() {
    const envPath = path.join(__dirname, '..', '.env');
    if (!fs.existsSync(envPath)) {
        console.error('[env] .env 文件不存在');
        process.exit(1);
    }
    const content = fs.readFileSync(envPath, 'utf-8');
    content.split('\n').forEach(line => {
        const trimmed = line.trim();
        if (!trimmed || trimmed.startsWith('#')) return;
        const eqIdx = trimmed.indexOf('=');
        if (eqIdx === -1) return;
        const key = trimmed.slice(0, eqIdx).trim();
        const value = trimmed.slice(eqIdx + 1).trim();
        if (key && value) process.env[key] = value;
    });
})();

const SUPABASE_URL = process.env.SUPABASE_URL;
const SUPABASE_SERVICE_ROLE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY;

if (!SUPABASE_URL || !SUPABASE_SERVICE_ROLE_KEY) {
    console.error('[init] 缺少 SUPABASE_URL 或 SUPABASE_SERVICE_ROLE_KEY');
    process.exit(1);
}

const BILI_HEADERS = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36",
    "Referer": "https://www.bilibili.com/",
};

// ========== 工具函数 ==========

function sleep(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
}

/** 校验 LRC 文本：至少有 minLines 行有效时间戳行 */
function isValidLRC(text, minLines = 5) {
    if (!text || typeof text !== 'string') return false;
    const timeRe = /\[\d{2}:\d{2}\.\d{2,3}\]/;
    const validLines = text.split('\n').filter(l => timeRe.test(l) && l.replace(timeRe, '').trim());
    return validLines.length >= minLines;
}

/** 从 Supabase 获取所有无歌词的歌曲 */
async function getSongsWithoutLyrics() {
    const url = `${SUPABASE_URL}/rest/v1/songs?select=id,title,singer&lrc_text=is.null&order=id.asc`;
    const resp = await fetch(url, {
        headers: {
            'apikey': SUPABASE_SERVICE_ROLE_KEY,
            'Authorization': `Bearer ${SUPABASE_SERVICE_ROLE_KEY}`,
        },
    });
    if (!resp.ok) {
        throw new Error(`查询歌曲失败: ${resp.status}`);
    }
    return resp.json();
}

/** 更新歌曲的 lrc_text */
async function updateLyrics(songId, lrcText) {
    const url = `${SUPABASE_URL}/rest/v1/songs?id=eq.${songId}`;
    const resp = await fetch(url, {
        method: 'PATCH',
        headers: {
            'apikey': SUPABASE_SERVICE_ROLE_KEY,
            'Authorization': `Bearer ${SUPABASE_SERVICE_ROLE_KEY}`,
            'Content-Type': 'application/json',
            'Prefer': 'return=minimal',
        },
        body: JSON.stringify({ lrc_text: lrcText }),
    });
    return resp.ok;
}

// ========== LRC API 搜索（多源尝试） ==========

/**
 * 搜索源 1: lrcshare 系列 API
 * 歌名 + 歌手 → 最佳匹配 LRC
 */
async function searchLRCMusic(song) {
    const query = `${song.title} ${song.singer || ''}`.trim();
    const encoded = encodeURIComponent(query);

    // 尝试多个公开 LRC API
    const apis = [
        {
            name: 'lrclib',
            url: `https://lrclib.net/api/search?q=${encoded}`,
            extract: async (data) => {
                // data is array, take first with syncedLyrics
                if (!Array.isArray(data) || data.length === 0) return null;
                const match = data.find(d => d.syncedLyrics);
                return match ? match.syncedLyrics : null;
            },
        },
        {
            name: 'lrclib-direct',
            url: `https://lrclib.net/api/get?track_name=${encodeURIComponent(song.title)}${song.singer ? '&artist_name=' + encodeURIComponent(song.singer) : ''}`,
            extract: async (data) => {
                if (!data || !data.syncedLyrics) return null;
                return data.syncedLyrics;
            },
        },
    ];

    for (const api of apis) {
        try {
            console.log(`  → 尝试 ${api.name}: ${api.url.substring(0, 80)}...`);
            const resp = await fetch(api.url, {
                headers: { ...BILI_HEADERS, 'Accept': 'application/json' },
            });
            if (!resp.ok) continue;

            const data = await resp.json();
            const lrc = await api.extract(data);
            if (lrc && isValidLRC(lrc)) {
                console.log(`  ✓ ${api.name} 匹配成功 (${lrc.split('\n').length} 行)`);
                return lrc;
            }
        } catch (err) {
            console.log(`  ✗ ${api.name} 请求失败: ${err.message}`);
        }
    }

    return null;
}

// ========== 主流程 ==========

async function main() {
    console.log('🎵 歌词自动匹配脚本\n');

    const songs = await getSongsWithoutLyrics();
    console.log(`找到 ${songs.length} 首无歌词的歌曲\n`);

    if (songs.length === 0) {
        console.log('所有歌曲都已有歌词，无需匹配。');
        return;
    }

    let success = 0;
    let failed = 0;

    for (let i = 0; i < songs.length; i++) {
        const song = songs[i];
        const label = `[${i + 1}/${songs.length}] #${song.id} ${song.title}` +
            (song.singer ? ` — ${song.singer}` : '');
        console.log(label);

        const lrc = await searchLRCMusic(song);

        if (lrc) {
            const updated = await updateLyrics(song.id, lrc);
            if (updated) {
                success++;
                console.log(`  ✓ 已写入数据库`);
            } else {
                failed++;
                console.log(`  ✗ 写入数据库失败`);
            }
        } else {
            failed++;
            console.log(`  ✗ 未找到匹配歌词`);
        }

        // 限速：避免请求过于频繁
        if (i < songs.length - 1) {
            await sleep(1500);
        }
    }

    console.log(`\n========== 完成 ==========`);
    console.log(`成功: ${success} 首`);
    console.log(`失败: ${failed} 首`);
    console.log(`总计: ${songs.length} 首`);
}

main().catch(err => {
    console.error('脚本执行失败:', err);
    process.exit(1);
});
```

- [ ] **Step 2: 运行脚本**

```bash
/d/softwa/nodejs/node scripts/fetch_lyrics.js
```

观察输出，确认匹配成功率和失败原因。

- [ ] **Step 3: 验证歌词数据**

```bash
# 查询已获取歌词的歌曲
SERVICE_KEY="<SUPABASE_SERVICE_ROLE_KEY>"
curl -s "https://orphftlwdwuvoscizndx.supabase.co/rest/v1/songs?select=id,title,lrc_text&lrc_text=not.is.null&limit=3" \
  -H "apikey: $SERVICE_KEY" -H "Authorization: Bearer $SERVICE_KEY"
```

- [ ] **Step 4: 端到端验证**

1. 启动服务器
2. 播放一首已获取到歌词的歌曲
3. 点击 `🎤` 打开歌词窗口
4. 验证歌词正确显示、当前行高亮、切换模式

- [ ] **Step 5: Commit**

```bash
git add scripts/fetch_lyrics.js
git commit -m "feat: add lyrics auto-matching script (lrclib API)"
```

---

## 验证清单（端到端）

全部 Task 完成后，逐项验证：

- [ ] 歌曲无歌词时，歌词窗口显示"暂无歌词"
- [ ] 歌曲有歌词时，播放过程中当前行高亮且自动滚动
- [ ] 竖条形模式显示约 10 行，当前行居中高亮
- [ ] 长条形模式显示 2 行，当前行大字高亮
- [ ] 模式切换按钮正常工作
- [ ] 关闭歌词窗口，主窗口不受影响
- [ ] 关闭主窗口，歌词窗口自动关闭
- [ ] 歌词窗口标题栏可拖拽
- [ ] 歌曲切换时，歌词窗口自动加载新歌词
- [ ] 窗口被浏览器拦截时提示用户
- [ ] LRC 解析异常时降级显示
